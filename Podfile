# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MessagesExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MessagesExtension
	
   pod 'Firebase/Database'
   pod "TextFieldEffects"
   pod 'SwiftyButton'
   pod 'RxSwift',    '~> 3.0'
   pod 'RxCocoa',    '~> 3.0'

   #pod 'CountdownLabel', :git => 'https://github.com/SeRG1k17/CountdownLabel.git'





end

target 'Send it' do
  # Comment the next line if you're not using Swift and don't want to use dynamicframeworks
  use_frameworks!
  # Pods for Send it
end
